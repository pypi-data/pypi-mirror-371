[project]
name = "mcp-spec"
version = "0.1.9"
description = "mcp for spec generation"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.10.6",
]

[project.scripts]
mcp-spec = "mcp_spec.main:main"