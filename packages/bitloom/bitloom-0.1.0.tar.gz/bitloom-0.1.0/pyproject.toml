[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bitloom"
version = "0.1.0"
description = "Minimal Bitloom lang processor"
authors = [{name="mav"}]
readme = "README.md"
license = {text="MIT"}
