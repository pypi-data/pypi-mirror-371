[project]
name = "greenlamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "greenlampgh", email = "greenlampgh@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "maturin>=1.9.3",
    "twine>=6.1.0",
]

[tool.maturin]
python-packages = ["greenlamp"]
python-source = "src"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
