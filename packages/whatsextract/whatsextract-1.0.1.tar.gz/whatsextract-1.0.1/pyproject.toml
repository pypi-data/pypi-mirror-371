[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "whatsextract"
version = "1.0.1"
description = "WhatsExtract API client (sync + async)"
readme = "README.rst"
requires-python = ">=3.9"
dependencies = [
  "requests>=2.31.0",
  "aiohttp>=3.9.0",
]
# pandas is optional; install later if you need the DataFrame helper

[project.urls]
Homepage = "https://example.com"
