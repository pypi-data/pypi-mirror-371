[project]
name = "hgc-scripts"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = []  # keep light; per-tool deps can live here or be optional

[project.scripts]
svg-extract-images = "scripts.extract_svg_images:main"
