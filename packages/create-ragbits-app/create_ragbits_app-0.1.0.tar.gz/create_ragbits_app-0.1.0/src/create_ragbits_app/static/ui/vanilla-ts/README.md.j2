# {{ ui_project_name }}

A TypeScript UI for Ragbits built with Vite.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Usage

This UI connects to the Ragbits API running on localhost:8000 by default.
You can modify the base URL in the RagbitsClient initialization in `src/main.ts`.

The template includes:
- Vite for fast development and building
- TypeScript support
- @ragbits/api-client for API integration

## Features

- Hot Module Replacement (HMR)
- TypeScript support
- Ragbits API client integration
- Minimal setup with no unnecessary dependencies
