# {{ ui_project_name }}

A React UI for Ragbits built with Vite and TypeScript.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Usage

This UI connects to the Ragbits API running on localhost:8000 by default.
You can modify the base URL in the RagbitsProvider component in `src/App.tsx`.

The template includes:
- Vite for fast development and building
- React 19 with TypeScript
- ESLint for code quality
- @ragbits/api-client and @ragbits/api-client-react for API integration

## Features

- Hot Module Replacement (HMR)
- TypeScript support
- ESLint configuration
- Ragbits API client integration
- React hooks for Ragbits API
