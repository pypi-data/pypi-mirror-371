from django.urls import path

from .views import *

urlpatterns = [
    path('signup', register_user, name='register_user'),
    path('login', login_user, name='login_user'),
    path('profile', user_detail, name='user_detail'),
    path('edit_user', edit_info, name='edit_info'),
]
