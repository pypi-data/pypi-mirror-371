[project]
name = "morten"
version = "0.1.0"
description = "A lossless TOML serde library"
readme = "README.md"
authors = [
    { name = "Bartosz Sokorski", email = "bartosz.sokorski@stxnext.pl" }
]
requires-python = ">=3.9"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/morten"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
