name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (black)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install black
        run: |
          python -m pip install --upgrade pip
          pip install black==25.1.0
      - name: Check formatting
        run: |
          black --check --diff ./src/umaapy

  # test:
  #   name: Test (pytest)
  #   needs: [lint]
  #   runs-on: ubuntu-latest
  #   env:
  #     # If you configure a license on the runner, set RTI_LICENSE_FILE or NDDSHOME accordingly.
  #     PYTHONPATH: src
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.13'
  #     - name: Install package and test dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -e .[tests]
  #         pip install pytest-timeout
  #     - name: Run tests
  #       run: |
  #         pytest --maxfail=1 -q --cov --cov-report=xml:coverage.xml --cov-report=term-missing --junitxml=report.xml
  #     - name: Upload test reports
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: test-reports
  #         path: |
  #           coverage.xml
  #           report.xml
  #           .pytest_cache/
