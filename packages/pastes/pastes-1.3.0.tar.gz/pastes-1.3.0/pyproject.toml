[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pastes"
version = "1.3.0"
authors = [
  { name="RimMirK", email="me@RimMirK.pp.ua" }
]
description = "Minimalistic Python client for pastes.dev"
readme = "README.md"
license = { file = "LICENCE" }
requires-python = ">=3.8"
dependencies = ["requests"]
