[project]
name = "qstrings"
version = "0.2.0"
description = "Q-strings: query anything"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "autoregistry>=1.1.2",
    "duckdb>=1.3",
    "sqlglot>=27.6.0",
]

[project.scripts]
q = "qstrings.cli:app"

[dependency-groups]
cli = [
    "cyclopts>=3.22.5",
]
dev = [
    "pytest>=8.4.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
