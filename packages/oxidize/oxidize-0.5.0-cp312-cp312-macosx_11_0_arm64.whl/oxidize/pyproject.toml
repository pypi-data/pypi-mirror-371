[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oxidize"
version = "0.5.0"
description = "High performance xml to json parsing"
requires-python = ">=3.9"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
bindings = "pyo3"