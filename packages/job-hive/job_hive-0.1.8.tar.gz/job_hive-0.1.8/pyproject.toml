[tool.poetry]
name = "job-hive"
version = "0.1.8"
description = "一个极简的分布式任务模块"
authors = ["YUNHAI <bybxbwg@foxmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
redis = "^6.4.0"

[tool.poetry.extras]
redis = ["redis"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
