[project]
name = "sparklingasterisk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sparklingasteriskgh", email = "rahulbhadani@email.arizona.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "twine>=6.1.0",
]

[tool.maturin]
module-name = "sparklingasterisk._core"
python-packages = ["sparklingasterisk"]
python-source = "src"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
