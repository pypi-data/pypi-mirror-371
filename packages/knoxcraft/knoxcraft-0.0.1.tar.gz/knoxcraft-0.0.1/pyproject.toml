[project]
name = "knoxcraft"
version = "0.0.1"
description = "Python client code for Knoxcraft Mod. Basically 3D Logo for Minecraft."
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Jaime Spacco"}]
dependencies = [
    "requests"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"