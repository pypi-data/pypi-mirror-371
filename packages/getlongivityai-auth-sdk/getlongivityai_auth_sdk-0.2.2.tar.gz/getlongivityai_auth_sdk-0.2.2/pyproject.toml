[project]
name = "getlongivityai-auth-sdk"
version = "0.2.2"
description = "Python Library to intract with Auth Service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pyjwt>=2.10.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"