[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "wordss"      # change to your package name
version = "0.1.5"
description = "A simple python package used to count letters,vowels,consonents of a word"
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE.txt" }
authors = [
    { name = "Vijayasarathi", email = "vijayasarathi871@gmail.com" }
]
dependencies = [
    # Example dependency: "requests>=2.0"
]
