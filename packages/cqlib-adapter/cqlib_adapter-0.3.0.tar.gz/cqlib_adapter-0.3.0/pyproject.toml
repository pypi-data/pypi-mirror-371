[project]
name = "cqlib-adapter"
description = "Cqlib adapter"
authors = [
    { name = "Gao Jianjian", email = "jianjian001@outlook.com" },
    { name = "Kan Yin", email = "kanyin@chinatelecom.cn" }
]
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
dynamic = ["version", "dependencies"]
classifiers = [
    "Environment :: Console",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries"
]

keywords = [
    "cqlib",
    "cqlib adapter",
    "pennylane",
    "quantum circuit",
    "quantum computing",
    "quantum programming language",
    "quantum",
    "sdk",
]

[project.entry-points."pennylane.plugins"]
"cqlib.device" = "cqlib_adapter.pennylane_ext.device:CQLibDevice"

[tool.setuptools]
packages = ["cqlib_adapter", "cqlib_adapter.qiskit_ext", "cqlib_adapter.pennylane_ext"]
include-package-data = true

[tool.setuptools.package-data]
"cqlib_adapter.pennylane_ext" = ["cqlib_config.toml"]

[tool.setuptools.dynamic]
version = { file = "cqlib_adapter/VERSION.txt" }
dependencies = { file = "requirements.txt" }

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"