[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyprojex"
version = "0.2.0"
description = "Python Project Manager"
authors = [
  { name="Chocolate Loves You", email="chocolatefr@gmail.com" }
]
readme = "README.md"
license = { text = "GNU" }
requires-python = ">=3.7"

dependencies = [
]

[project.urls]
"Homepage" = "https://github.com/frchocolate/projex"
"Bug Tracker" = "https://github.com/frchocolate/projex/issues"

[project.scripts]
projex = "projex.main:main"
