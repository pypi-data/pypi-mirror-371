[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "sk-linearregression"              
version = "0.4.0"
description = "My own Linear Regression under the sk.* namespace"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "Shahabaj Khan", email = "shahabaj773@gmail.com" }]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/sk"]
