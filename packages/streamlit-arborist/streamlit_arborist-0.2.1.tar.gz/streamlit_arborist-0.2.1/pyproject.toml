[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "streamlit-arborist"
dynamic = ["version"]
description = "Streamlit component for creating tree views"
readme = "README.md"
authors = [{ name = "Gabriel Mello Silva", email = "gabriel.zyx@gmail.com" }]
license-files = ["LICENSE"]
requires-python = ">=3.9"
# Version 1.36.0 implements `on_change` for `CustomComponent`
dependencies = ["streamlit>=1.36.0"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Plugins",
  "Environment :: Web Environment",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]

[project.urls]
homepage = "https://github.com/gabriel-msilva/streamlit-arborist"
repository = "https://github.com/gabriel-msilva/streamlit-arborist"
documentation = "https://streamlit-arborist.readthedocs.io"

[dependency-groups]
dev = [
  "ipython>=8.18.1",
  "pre-commit==4.2.0",
  { include-group = "tests" },
]
tests = [
  "pytest==7.4.0",
  "playwright==1.48.0",
  "pytest-playwright-snapshot==1.0",
  "pytest-rerunfailures==12.0",
  "requests==2.31.0",
]
docs = ["sphinx==7.4.7", "sphinx-rtd-theme==3.0.2"]

[tool.hatch.build]
artifacts = ["streamlit_arborist/frontend/build/**"]
exclude = ["streamlit_arborist/frontend/**"]

[tool.hatch.version]
path = "streamlit_arborist/__init__.py"

[tool.pytest.ini_options]
testpaths = ["e2e"]
addopts = "-vv"

[tool.ruff]
target-version = "py39"

[tool.ruff.lint]
select = [
  "E4",
  "E7",
  "E9",  # pycodestyle
  "F",   # Pyflakes
  "I",   # isort
  "A",   # flake8-builtins
  "B",   # flake8-bugbear
  "SIM", # flake8-simplify
]
unfixable = ["F401"]
