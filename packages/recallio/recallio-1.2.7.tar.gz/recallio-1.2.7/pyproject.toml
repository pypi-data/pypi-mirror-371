[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "recallio"
version = "1.2.7"
description = "Python client for the Recallio API"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Recallio Team", email = "support@recallio.com"}]
dependencies = ["requests>=2.31"]

[project.urls]
Homepage = "https://app.recallio.ai"
