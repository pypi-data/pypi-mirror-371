[build-system]
requires = ["scikit-build-core"]
build-backend = "scikit_build_core.build"

[project]
name = "fancy"
version = "0.0.1"
dynamic = ["readme"]

[tool.scikit-build]
experimental = true
metadata.readme.provider = "non_existent"
