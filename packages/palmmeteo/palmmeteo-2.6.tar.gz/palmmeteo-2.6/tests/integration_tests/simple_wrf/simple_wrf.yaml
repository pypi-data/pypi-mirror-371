case: simple_wrf

tasks:
    - wrf
    - wrf_rad

domain:
    nz: 25

paths:
    base: tests/integration_tests/simple_wrf
    snapshot:
        setup_model:  'METEO/setup{domain}.pickle'
        import_data:  'METEO/import{domain}.pickle'
        hinterp:      'METEO/hinterp{domain}.pickle'
        vinterp:      'METEO/vinterp{domain}.pickle'

simulation:
    origin_time: 2018-07-19 10:00:00
    length: 3 h

hinterp:
    #validate: true

output:
    overwrite: true
    #check_mass_balance: true

verbosity: 2
